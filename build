# Build script generated by gb: http://go-gb.googlecode.com
# gb provides configuration-free building and distributing

echo "Build script generated by gb: http://go-gb.googlecode.com" 
if [ "$1" = "goinstall" ]; then
echo Running goinstall \
&& echo "goinstall github.com/garyburd/twister/web" \
&& goinstall github.com/garyburd/twister/web \
&& echo "goinstall github.com/ziutek/kview" \
&& goinstall github.com/ziutek/kview \
&& echo "goinstall github.com/garyburd/twister/server" \
&& goinstall github.com/garyburd/twister/server \

else
echo Building \
&& echo "(in .)" && cd . && make $1 && cd - > /dev/null \
&& echo "(in osgb36)" && cd osgb36 && make $1 && cd - > /dev/null \
&& echo "(in lv03p)" && cd lv03p && make $1 && cd - > /dev/null \
&& echo "(in examples/conv)" && cd examples/conv && make $1 && cd - > /dev/null \
&& echo "(in examples/cartconvserv)" && cd examples/cartconvserv && make $1 && cd - > /dev/null \
&& echo "(in bmn)" && cd bmn && make $1 && cd - > /dev/null \
&& echo "(in examples/httptempltest)" && cd examples/httptempltest && make $1 && cd - > /dev/null \
&& echo "(in examples/geotrans)" && cd examples/geotrans && make $1 && cd - > /dev/null \
&& echo "(in examples/templtest)" && cd examples/templtest && make $1 && cd - > /dev/null \
&& echo "(in examples/cartconvserv)" && cd examples/cartconvserv && make $1 && cd - > /dev/null \

fi

# The makefiles above are invoked in topological dependence order
